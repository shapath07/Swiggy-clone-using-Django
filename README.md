File setup
  - by using git command     git clone <repository_url>  in your desired directory // type this command in your terminal //
  - and in same folder add venv in which django framework and pillow library has installed 
